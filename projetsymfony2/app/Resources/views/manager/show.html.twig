{% extends 'base.html.twig' %}

{% block body %}
    <h1>Manager</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manager.id }}</td>
            </tr>
            <tr>
                <th>Nomanager</th>
                <td>{{ manager.nomanager }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ manager.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ manager.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('manager_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('manager_edit', { 'id': manager.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
